kind = "web"
previewPath = "/"
title = "NearBuy"
version = "1.0.0"
id = "artifacts/nearbuy"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19010

[services.development]
run = "pnpm --filter @workspace/nearbuy run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nearbuy", "run", "build" ]
publicDir = "artifacts/nearbuy/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19010"
BASE_PATH = "/"
